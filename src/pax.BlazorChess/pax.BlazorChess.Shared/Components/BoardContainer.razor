@using pax.chess

<div class="w-50">
    <BoardPositionComponent Position="Game.CurrentPosition" Game="Game" />
</div>

@code {
    [Parameter, EditorRequired]
    public required ChessGame Game { get; set; }
}
